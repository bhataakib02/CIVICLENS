# CivicLens backend — runtime + dev dependencies (pinned)
# Python 3.11+ target.
# For local dev: pip install -e .[dev]
# For production images: pip install -r requirements.txt (pgserver excluded by Dockerfile)

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20   # multipart/form-data parsing for document upload

# --- Data validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- ORM / DB ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3     # psycopg v3 (postgresql+psycopg:// driver)
pgvector==0.3.6            # pgvector SQLAlchemy type + vector ops

# --- Knowledge / RAG ingestion ---
pypdf==5.1.0               # PDF text + page extraction (pure-python, no system deps)

# --- Security ---
argon2-cffi==23.1.0
PyJWT==2.10.1

# --- Realtime / caching (optional at runtime; required when realtime_provider=redis) ---
redis==5.2.1

# --- Testing (dev only — excluded from production Docker image) ---
pytest==8.3.4
httpx==0.28.1
anyio[trio]==4.7.0         # async test support
pgserver==0.1.4            # bundled PostgreSQL binary for real (non-mocked) integration tests
PyYAML==6.0.2              # OpenAPI contract comparison in tests
